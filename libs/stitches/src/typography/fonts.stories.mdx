import { Meta, Source } from '@storybook/addon-docs';
import { Fonts } from '../story-components/Fonts';

<Meta title="Stitches / Typography / Font Families" />

# Fonts

The stitches library includes only one font, it is the inter font and it is automatically applied to all elements using the global styles.

## Demo

<Fonts />

import { type NextPage } from 'next';

import { Typography } from '@noodle/ui';

const Home: NextPage = () => {
  return (
    <main>
      <h1>Home page</h1>
      <Typography />
    </main>
  );
};

export default Home;

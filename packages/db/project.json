{
  "sourceRoot": "packages/db",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["prisma generate", "tsc -p tsconfig.build.json"],
        "parallel": false,
        "cwd": "packages/db"
      }
    }
  }
}

{
  "name": "noodle",
  "version": "0.1.0",
  "description": "Rethinking Student Productivity",
  "license": "AGPL-3.0",
  "author": "Ahmed Elsakaan",
  "type": "module",
  "scripts": {
    "build": "pnpm nx affected -t build --parallel=5",
    "build-storybook": "pnpm nx affected -t build-storybook --parallel=5",
    "build:pkgs": "pnpm nx affected -t build --exclude web --parallel=5",
    "clean": "pnpm clean:bundles && del pnpm-lock.yaml **/node_modules && pnpm install",
    "clean:bundles": "del **/coverage **/storybook-static **/tsconfig.tsbuildinfo **/dist **/.next",
    "dev": "pnpm nx run-many -t dev --parallel=99 --output-style=stream",
    "format:check": "prettier --check .",
    "format:write": "prettier --write .",
    "lint": "pnpm nx affected -t lint --parallel=5",
    "lint:fix": "pnpm nx affected -t lint:fix --parallel=5",
    "prepare": "husky install",
    "start": "pnpm nx run-many -t start --parallel=99 --output-style=stream",
    "storybook": "pnpm nx storybook storybook --output-style=stream",
    "test": "pnpm nx affected -t test --parallel=5",
    "test:coverage": "pnpm nx affected -t test:coverage --parallel=5 --output-style=stream",
    "test:ui": "pnpm nx run-many -t test:ui --parallel=99 --output-style=stream",
    "test:watch": "pnpm nx run-many -t test:watch --parallel=99 --output-style=stream",
    "typecheck": "pnpm nx affected -t typecheck --parallel=5"
  },
  "devDependencies": {
    "@commitlint/cli": "^17.6.5",
    "@commitlint/config-conventional": "^17.6.5",
    "@ianvs/prettier-plugin-sort-imports": "^4.0.1",
    "@noodle/tsconfig": "workspace:^",
    "@types/eslint": "^8.40.0",
    "@types/node": "^20.2.5",
    "@types/prettier": "^2.7.3",
    "del-cli": "^5.0.0",
    "eslint": "^8.41.0",
    "eslint-config-noodle": "workspace:^",
    "husky": "^8.0.3",
    "lint-staged": "^13.2.2",
    "nx": "16.3.1",
    "nx-cloud": "16.0.5",
    "prettier": "^2.8.8",
    "prettier-plugin-jsdoc": "^0.4.2",
    "prettier-plugin-packagejson": "^2.4.3",
    "prettier-plugin-tailwindcss": "^0.3.0",
    "typescript": "^5.0.4"
  },
  "packageManager": "pnpm@8.6.0",
  "engines": {
    "node": ">=18.16.0",
    "pnpm": ">=8.6.0"
  },
  "pnpm": {
    "patchedDependencies": {
      "@types/testing-library__jest-dom@5.14.6": "patches/@types__testing-library__jest-dom@5.14.6.patch"
    }
  }
}
